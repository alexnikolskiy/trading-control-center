# Contract: Platform To Office

`trading-office` integrates with platform capabilities through SDK/API surfaces.

## Rules

- Operator UI behavior should use stable public contracts.
- UI-visible platform behavior changes require release notes or migration notes.
- Operational actions should carry correlation identifiers into platform telemetry.

