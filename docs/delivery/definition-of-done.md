# Definition Of Done

A cross-repo change is done when:

- the source-of-truth repository is updated;
- affected SDK contracts, examples, and version metadata are updated;
- affected consumers are updated or explicitly marked unaffected;
- validation gates have been run;
- observability and rollback impact are known;
- rollout, migration, or changelog notes exist when behavior changed.

